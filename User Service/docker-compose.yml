version: '3.8'

services:
  userservice:
    image: 11112002/userservice:latest
    networks:
      - backend
    build:
      context: .
      dockerfile: User Service/Dockerfile
networks:
  backend:
    name: backend
    external: true
    driver: bridge
